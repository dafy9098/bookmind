# BookMind — Hybrid Book Recommendation System

Dataset: goodbooks-10k (zygmuntz/goodbooks-10k)
Methods: SVD (Collaborative Filtering) + TF-IDF (Content-Based) — Weighted Hybrid

## Run locally
pip install -r requirements.txt
streamlit run app.py

## Deploy
Streamlit Cloud → connect repo → set main file: app.py